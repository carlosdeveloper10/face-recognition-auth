pipeline {
    agent {
        label 'linux-mint'
    }

	stages {
	    stage('Run unit test') {
    	   steps {
    	   		sh 'mvn test'
   	    	}
    	}
	}
	
	post {
	    always {
    	    junit 'target/surefire-reports/TEST-*xml'
    	}
	}
}
