spring:
  data:
    rest:
      base-path: /api
  datasource:
    username: ${dbusername:carlos}
    password: ${dbpassword:12345678}
    url: ${dburl:jdbc:postgresql://172.18.0.2:5432/fra_dbho}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: ${port:5000}